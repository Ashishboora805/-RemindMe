# Bundled images

Placeholder for any images shipped with the app (empty states, onboarding art).
User-captured photos are **not** stored here — they are copied into
`Documents/attachments/images` at runtime by `AttachmentService`.
